# spring: 
#   application: 
#     name: Config-Default

# eureka:
#   instance:
#     prefer-ip-address: true
#   client:
#     fetch-registry: true
#     registry-with-eureka: true
#     service-url:
#       defaultZone: http://localhost:8761/eureka

eureka:
  instance:
    prefer-ip-address: true
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/hotel_microservices_db
    username: root
    password: Ironman@123

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
